# Esteg.IO
A simple web app for steganography that lets users hide and extract secret messages inside images directly in the browser. It encodes text into image pixels without changing the visual appearance and allows decoding from any previously encoded image — all with privacy, no uploads, and no installation required.
